@inject ThemeService ThemeService

<RadzenDropDown TValue="string" 
                Value=@ThemeService.Theme 
                ValueChanged=@ChangTheme
                ValueProperty=@nameof(Theme.Value)
                Data=@_freeLightThemes 
                TextProperty=@nameof(Theme.Text)
                Style="margin-right:5px;" 
/>

@code {
    private IEnumerable<Theme> _freeLightThemes = Themes.Free
        .Where(theme => !theme.Value.Contains("dark", StringComparison.OrdinalIgnoreCase)).ToList();

    private void ChangTheme(string theme)
    {
        ThemeService.SetTheme(theme);
    }
}